import 'package:flutter/material.dart';

class EditTodosScreen extends StatelessWidget {
  const EditTodosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Edit Todos Screen"),
      ),
    );
  }
}
